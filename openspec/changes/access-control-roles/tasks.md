## Tasks

- [x] 1. OpenSpec proposal / design / deltas auth, admin, dashboard, system
- [ ] 2. Schema `role` + `disabled_at` + `visitor_enabled` ; store users / sessions
- [ ] 3. `requireViewer` / `requireRole` ; routes config, users, access
- [ ] 4. Web : AuthContext, porte d’entrée, Layout, admin par rôle
- [ ] 5. Tests + sync specs courantes, baseline, README, règles Cursor

## Tasks

- [x] 1. OpenSpec proposal / design / deltas auth, admin, dashboard, system
- [x] 2. Schema `role` + `disabled_at` + `visitor_enabled` ; store users / sessions
- [x] 3. `requireViewer` / `requireRole` ; routes config, users, access
- [x] 4. Web : AuthContext, porte d’entrée, Layout, admin par rôle
- [x] 5. Tests + sync specs courantes, baseline, README, règles Cursor
